/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Gradle wrapper scripts and JAR are intentionally tracked so clean checkouts
# can reproduce Android builds without relying on locally generated wrapper files.

# Never commit Android release signing material.
key.properties
**/*.keystore
**/*.jks
